# name: Manual workflow # github actionの名前

# on:
#   workflow_dispatch: # トリガー
#     inputs: # 入力
#       name:
#         description: 'Person to greet' # 説明
#         default: 'World' # 初期の文字
#         required: true # 空文字の許容

# jobs:
#   greet: # ジョブ名（自由記述）
#     runs-on: ubuntu-latest # ジョブが実行される環境
#     steps:
#     # Runs a single command using the runners shell
#     # ランナーシェルを使用して単一のコマンドを実行します
#     - name: Send greeting # 実行のまとまり名
#       run: |
#       echo "Hello ${{ github.event.inputs.name }}" # 実際に実行されるもの
